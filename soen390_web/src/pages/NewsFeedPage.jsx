import React from "react";

export default function NewsFeedPage(){

    return(
        <div>
            The newsfeed page, the first page a user would see after a login attempt
        </div>
    )
}